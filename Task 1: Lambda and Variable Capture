List<Action> actions = new List<Action>();

for (int i = 1; i <= 5; i++)
{
    int temp = i; // створюємо копію змінної
    actions.Add(() => Console.WriteLine(temp));
}

foreach (var action in actions)
{
    action();
}
